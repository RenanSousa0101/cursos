// const taskModel = require('../../../src/models/taskModel');
// const listTask = taskModel.getAllTasks();

// listTask.forEach(task => {
//     task.tasks.forEach(tasks => {
//         if(tasks.check === true){
//             let span = document.getElementById(tasks.id);
//             span.classList.add('check');
//         }
//     })
// })