<!-- Utilizando recursos da linguagem JavaScript dentro do HTML
É possível também usar outras estruturas do JS, desde que utilizemos a tag especial 
do EJS <% %> a cada trecho de código não-HTML.
Através disso podemos usar condicionais, repetições, e qualquer outro recurso da 
linguagem normalmente. -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
    <%= title %>
    </title>
</head>

<body>
    <h1>Renderizando HTML Dinâmico com EJS</h1>
    <p>
        <%= message %>

        <% if (1===1) { %>
            <p>verdadeiro</p>
        <% } else { %>
            <p>falso</p>
        <% } %>

        <ul>
        <% for( let index=0; index <=10; index++ ) { %>
            <li>
                4 * <%= index %> = <%= 4 * index %>
            </li>
        <% } %>
        </ul>
    </p>
</body>

</html>