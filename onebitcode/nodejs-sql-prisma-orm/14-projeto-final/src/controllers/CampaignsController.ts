import { Handler } from "express";

export class CampaignsController {
    index: Handler = async (req, res, next) => {
        try {
        
        } catch (error) {
            next(error)
        }
    }
}